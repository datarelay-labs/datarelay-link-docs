---
title: "Windows Agent Host"
description: "Enroll and operate a Windows Managed Host running the DRLink Agent."
sidebarTitle: "Windows Agent Host"
---

# Windows Agent Host

Windows is an Agent Host platform; the DRLink Server remains Linux-based.

Use the enrollment command generated by the Server. Do not use obsolete v2.2 client commands or copy old fixed bootstrap URLs.

## Enroll

On the Server:

```text
set enrollment zero-touch
```

Run the generated Windows installation/enrollment command according to the release or candidate package instructions.

## Verify

Open the DataRelay Link CLI on the Windows Agent Host and verify:

```text
show status
show agent
system info
system diagnostics
```

## Remote Services

Create Remote Services on the Windows Agent Host. RDP is represented as TCP/3389 through a Service Object; DataRelay Link does not enable RDP, create Windows credentials, or weaken Windows Firewall/NLA settings.

## Lifecycle

Use Agent lifecycle operations such as pause, resume, restart, product update, engine update, diagnostics, support bundle, and uninstall as exposed by the Windows CLI/package.
