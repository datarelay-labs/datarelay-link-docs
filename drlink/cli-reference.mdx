---
title: "CLI Reference"
description: "Canonical drlink command grammar and everyday operator commands."
---

# CLI Reference

<Note>
  This page describes the **v2.4 development target**. The CLI/AI Master in the product repository is authoritative when an abbreviated example here is unclear.
</Note>

## Common discovery

```text
?
help
help commands
<verb> ?
Tab
```

## Server — read

```text
show status
show managed-hosts
show managed-host <HOST>
show managed-host <HOST> remote-services
show enrollments
show network-objects
show network-groups
show service-objects
show service-groups
show remote-access
show internet-access
show ai-identities
show permission-objects
show permission-groups
show ai-access
show ai-access-log
```

## Server — create/edit

```text
set enrollment zero-touch
set enrollment manual
set enrollment bulk

set network-object <NAME>
set network-group <NAME>
set service-object <NAME>
set service-group <NAME>
set ai-identity <NAME>
set permission-object <NAME>
set permission-group <NAME>

set remote-access <RULE>
set internet-access <RULE>
set ai-access <RULE>
```

Bare named set enters Guided Create/Edit. A complete one-shot form skips the Wizard.

```text
set network-object github type fqdn value github.com
set service-object ssh type tcp port 22
set permission-object read-only permissions host-info,process-read,file-read
set remote-access block-partner mode blacklist source partner destination prod service ssh enabled
```

## Policy lifecycle

```text
set remote-access enabled
set remote-access disabled
unset remote-access <RULE>
unset remote-access policy

set internet-access enabled
set internet-access disabled
unset internet-access <RULE>
unset internet-access policy

set ai-access enabled
set ai-access disabled
unset ai-access <RULE>
unset ai-access policy
```

Rules are not ordered and have no per-rule ALLOW/DENY action.

## Policy test

```text
test remote-access source <SOURCE> destination <DESTINATION> service <SERVICE>
test internet-access source <SOURCE> destination <DESTINATION> service <SERVICE>
test ai-access source <AI_IDENTITY> destination <DESTINATION> permission <PERMISSION>
```

## Server — system/configuration

```text
system status
system version
system diagnostics
system audit
system revisions
system backup
system restore <FILE>

test configuration <FILE|->
system diff configuration <FILE|->
system apply configuration <FILE|->
system export configuration <FILE>

system update
system support-bundle
system uninstall
```

## Agent Host

```text
show status
show agent
show remote-services
show remote-service <NAME>

set remote-service <NAME>
unset remote-service <NAME>

system info
system pause
system resume
system restart
system update product
system update engine
system diagnostics
system support-bundle

test configuration <FILE|->
system diff configuration <FILE|->
system apply configuration <FILE|->
system export configuration <FILE>
```

Remote Service supports TCP and Fixed TCP Service Objects. UDP and Service Groups are rejected for Remote Service use.
