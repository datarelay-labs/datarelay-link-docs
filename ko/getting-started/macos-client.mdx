---
title: "macOS Agent Host"
description: "Apple Silicon macOS Managed Host에서 DRLink Agent를 등록하고 운영합니다."
sidebarTitle: "macOS Agent Host"
---

# macOS Agent Host

지원되는 macOS 경로에서는 Apple Silicon Mac을 Agent Host로 사용합니다.

오래된 고정 bootstrap URL을 복사하지 말고 Server가 발급한 Zero-Touch 명령을 사용합니다.

## 등록

Server에서:

```text
set enrollment zero-touch
```

발급된 명령을 대상 Mac에서 필요한 관리자 권한으로 실행합니다.

## 확인

```text
show status
show agent
system info
system diagnostics
```

## Lifecycle

```text
system pause
system resume
system restart
system update product
system update engine
system uninstall
```

DataRelay Link는 Remote Login을 활성화하거나 macOS 사용자를 생성하지 않습니다.
