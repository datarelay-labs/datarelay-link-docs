---
title: "Remote Service"
description: "Agent Host에서 로컬 또는 LAN TCP/Fixed TCP 연결을 생성합니다."
sidebarTitle: "Remote Service"
---

# Remote Service

Remote Service는 Agent Host가 소유하는 실제 연결 객체입니다.

```text
Agent Host
+ destination
+ Service Object
→ Remote Service
→ DataRelay Link endpoint
```

Agent Host에서 생성합니다.

```text
set remote-service ssh-access destination this-host service ssh enabled
show remote-service ssh-access
```

this-host 대신 Agent가 접근 가능한 다른 Host를 지정하면 현재 Agent Host가 Relay Host가 됩니다.

Remote Service는 TCP와 Fixed TCP Service Object를 지원하고 UDP와 Service Group은 거부합니다.

상태는 HEALTHY, DEGRADED, DISABLED입니다. 일시적인 연결 또는 sync 문제는 유효한 Remote Service 설정을 자동 삭제하지 않습니다.
